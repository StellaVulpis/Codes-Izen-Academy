[메서드(Method)]
-메서드는 기능을 갖지만 독자적으로 실행될 수 없고 반드시 main에서 실행시켜야 한다.
-한가지 기능을 가짐
-모든 메서드는 ()가 존재
-다른말로는 function(함수)이라고 한다
----------------------------------------------------------------------------
-this는 현재 클래스에서 사용 (필드의 변수들에 사용한다. 메소드 변수와 구분지을 수 있다.)
class
{
  int num=0;
  void Method()
  {
     int num=10;
     this.num = num;
  }
}
-super는 상속받은 클래스에서 사용
